#!/usr/bin/env python3
# This script will logout from qtile

from libqtile.command import Client
import getpass
import subprocess
#try:
client = Client()
client.restart()
# except:
#     subprocess.Popen(["zenity", "--error", "Could not restart!"])
    
